## 1. 配置包模型与读写

- [x] 1.1 设计并实现导出配置包的数据结构与版本字段，支持序列化账号元数据、使用历史和认证快照。
- [x] 1.2 实现导入前的配置包校验逻辑，覆盖结构、版本和账号 ID 冲突检查。

## 2. CLI 导出与导入

- [x] 2.1 增加导出配置命令，将当前已保存账号写入单个配置包文件，并输出敏感文件提示。
- [x] 2.2 增加导入配置命令，在通过校验后恢复账号注册表和账号目录，但不修改 live `auth.json` / `config.toml`。

## 3. 回归验证

- [x] 3.1 增加测试，覆盖成功导出配置包和成功导入恢复账号配置。
- [x] 3.2 增加测试，覆盖坏配置包拒绝导入、冲突账号拒绝导入，以及导入不影响 live 配置。
