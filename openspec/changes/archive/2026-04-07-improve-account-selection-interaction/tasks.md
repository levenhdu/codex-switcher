## 1. 使用历史数据模型

- [x] 1.1 扩展账号注册表读写结构，为账号增加可选的 `last_used_at` 和 `use_count` 字段。
- [x] 1.2 在账号切换成功后更新目标账号的最近使用时间和累计使用次数，确保失败切换不会污染历史。

## 2. 交互式选择排序

- [x] 2.1 在账号 UI 层抽出统一的账号排序函数，按最近使用时间、使用次数和稳定回退顺序生成交互式列表。
- [x] 2.2 将 `selectAccount` 和 `selectMultipleAccounts` 接入统一排序结果，保持现有当前账号标记和描述信息。

## 3. 回归验证

- [x] 3.1 增加测试，覆盖最近使用优先、同最近时间下按频次排序、无历史账号稳定排序。
- [x] 3.2 增加测试，覆盖切换成功后会写入 `last_used_at` 与递增 `use_count`。
